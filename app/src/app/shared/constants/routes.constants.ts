/**
 * The application's route names.
 */
export const APP_ROUTES = Object.freeze({
    consultation: 'consultation',
    login: 'login',
    admin: {
        teams: 'teams',
        results: 'results'
    }
})
